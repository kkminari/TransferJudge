# ============================================================
# 기본 (Mac 로컬·RunPod 공통)
# ============================================================
pandas>=2.0
numpy>=1.24
pyarrow>=14.0
python-dotenv>=1.0

# ============================================================
# Phase 0·1·2 — Profile/Teacher 생성용 (Mac 로컬)
# ============================================================
openai>=1.40
tiktoken>=0.7
datasets>=2.18         # HF Amazon Reviews 2023
huggingface-hub>=0.23  # Hub upload/download
sentence-transformers>=2.7  # Pilot HDBSCAN 임베딩
hdbscan>=0.8           # Pilot 클러스터링
scikit-learn>=1.3
matplotlib>=3.7
weasyprint>=60         # 보고서 PDF 생성

# ============================================================
# Phase 3 — Qwen3-14B QLoRA 파인튜닝 (RunPod GPU)
# ============================================================
# 주의: TRL SFTConfig의 max_length / processing_class / assistant_only_loss는
# transformers>=4.51 + trl>=0.16 (이상)에서 안정 지원 (Qwen3 공식 페이지 권장).
torch>=2.2,<2.6
transformers>=4.51,<4.55
peft>=0.13,<0.20
bitsandbytes>=0.44,<0.46
accelerate>=0.34,<0.40
trl>=0.16,<0.20
sentencepiece>=0.2
protobuf>=4.25
einops>=0.8

# Monitoring (선택, configs/judge_training.yaml에서 report_to로 토글)
wandb>=0.17

# ============================================================
# Phase 4·5 — 평가
# ============================================================
# (Phase 3와 동일 의존성 + 평가 utils)
